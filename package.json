{
	"name": "mal-rater",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Personal tool: score an anime backlog and write the scores back to MyAnimeList.",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.0.0",
		"typescript": "^5.9.3",
		"wrangler": "^4.0.0"
	}
}
